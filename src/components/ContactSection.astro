---
---

<section class="py-16 bg-primary text-secondary" id="contacto">
  <div class="container mx-auto px-4">
    <div class="max-w-4xl mx-auto">
      <h2 class="text-3xl font-bold text-center mb-12">Contacto</h2>
      <p class="text-center mb-10">
        Déjanos tus dudas en el formulario de contacto o escríbenos un email directamente. 
        Te responderemos a la brevedad.
      </p>
      
      <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
        <div>
          <form class="space-y-4">
            <div>
              <label for="name" class="block text-sm font-medium text-text mb-1">Nombre</label>
              <input
                type="text"
                id="name"
                name="name"
                class="w-full px-4 py-2 border border-gray-400 rounded-md focus:ring-accent focus:border-accent"
                required
              />
            </div>
            <div>
              <label for="email" class="block text-sm font-medium text-text mb-1">Email</label>
              <input
                type="email"
                id="email"
                name="email"
                class="w-full px-4 py-2 border border-gray-400 rounded-md focus:ring-accent focus:border-accent"
                required
              />
            </div>
            <div>
              <label for="phone" class="block text-sm font-medium text-text mb-1">Teléfono</label>
              <input
                type="tel"
                id="phone"
                name="phone"
                class="w-full px-4 py-2 border border-gray-400 rounded-md focus:ring-accent focus:border-accent"
              />
            </div>
            <div>
              <label for="message" class="block text-sm font-medium text-text mb-1">Mensaje</label>
              <textarea
                id="message"
                name="message"
                rows="4"
                class="w-full px-4 py-2 border border-gray-400 rounded-md focus:ring-accent focus:border-accent"
                required
              ></textarea>
            </div>
            <div>
              <button
                type="submit"
                class="w-full bg-amber-500 hover:bg-amber-600 text-text font-medium py-2 px-4 rounded-md transition-colors shadow-md"
              >
                Enviar mensaje
              </button>
            </div>
          </form>
        </div>
        
        <div class="bg-white p-6 rounded-lg shadow-sm">
          <div class="space-y-6">
            <div>
              <h3 class="text-lg font-semibold mb-2">DONDE ENCONTRARNOS</h3>
              <p class="text-gray-600">
                Te esperamos en nuestro showroom ubicado en Lavalle 2306, Once - CABA, Buenos Aires 7ºpiso, oficina 708.
                Recuerda registrarte previo a la visita.
              </p>
            </div>
            <div>
              <h3 class="text-lg font-semibold mb-2">HORARIO DE ATENCIÓN</h3>
              <p class="text-gray-600">Lunes a Viernes de 08:30 a 17:00</p>
            </div>
            <div>
              <h3 class="text-lg font-semibold mb-2">ESCRIBINOS UN EMAIL/LLAMANOS</h3>
              <p class="text-gray-600">fluydo@dolape.com.ar</p>
              <p class="text-gray-600">(54) 117165-0002</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
